[phases.setup]
nixPkgs = ['python311', 'python311Packages.pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[start]
cmd = 'gunicorn webapp.webapp.wsgi:application --bind 0.0.0.0:$PORT --timeout 300 --workers 2'
